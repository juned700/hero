<h1><%= @user.name %></h1>
<%= @user.email %>

<br><br>
<% if is_currect_user?(@user) %>
  <%= link_to "Edit Account", edit_user_path(@user), class: 'btn btn-primary btn-sm' %>

  <%= link_to "Delete Account", user_path(@user), method: :delete, class: 'btn btn-danger btn-sm' %>
<% end %>